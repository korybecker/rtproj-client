import { Box, Typography } from "@mui/material";

const Home = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h4">PRODUCTIVITY APP</Typography>
        </Box>
    );
};

export default Home;